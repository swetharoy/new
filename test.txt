/* This line be removed by git reset operation */
#include <stdio.h>
int main(void)
{
   int i;
   char *s[] = 
   {
      "Git tutorialspoint",
      "Tutorials Point"
   };

   for (i = 0; i < 5; ++i)
      
   printf("string lenght of %s = %d\n", s[i], my_strlen(s[i]));

   return 0;
}
